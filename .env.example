# BCET ATR — optional local overrides
#
# The app uses Supabase for auth and data. Connection is configured in
# src/lib/supabase.ts (URL + service role). For local experiments you can
# point that file at another project instead of editing env here.
#
# Legacy MongoDB seed (unused by the current app): npm run seed:mongodb
